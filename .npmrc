# vite-plugin-pwa@1.2.0 hasn't declared Vite 8 peer dep compat yet;
# it works fine with Vite 8 in practice. Remove once they release a
# version that lists ^8.0.0 in peerDependencies.
legacy-peer-deps=true
